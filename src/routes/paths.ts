const paths = {
  notFound: '*',
  root: '/',
  joinList: '/christmas',
  signSuccess: '/success',
  counterEndDate: '/christmas-day',
  counterIsOver: '/christmas-is-over',
};

export default Object.freeze(paths);
